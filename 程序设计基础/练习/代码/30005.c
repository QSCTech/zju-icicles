#include <stdio.h>
#include "genlib.h"

int main(void)
{
    int math = 87, eng = 72, comp = 93, average = 84;
    printf("math = %d, eng = %d, comp = %d, average = %d\n", math, eng, comp, average);
    return 0;
}
